schema: reporting_metrics_dev
tables:
  - name: int_active_tradies_count__tradie
    meta:
      owner: shumingpeh@hipagesgroup.com.au
    description: >-
      This table tracks the aggregate counts of active (and inactive) tradies each day
    s3_bucket: s3://au-com-hipages-offline-feature-store
    folder: int/reporting_metrics/int_active_tradies_count__tradie
    columns:
      - name: num_active_tradies
        description: counts of active tradies
        data_type: int
      - name: num_inactive_tradies
        description: counts of inactive tradies
        data_type: int
      - name: date_created
        description: partition date
        data_type: string
